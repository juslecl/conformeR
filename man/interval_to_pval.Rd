% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{interval_to_pval}
\alias{interval_to_pval}
\title{Convert prediction intervals to p-values}
\usage{
interval_to_pval(intervals_df)
}
\arguments{
\item{intervals_df}{A tibble with columns \code{cell_id}, \code{conf_group}, \code{gene},
and a logical column \code{covered} indicating whether the interval includes the value.}
}
\value{
A tibble with one row per \code{cell_id} and a p-value.
}
\description{
Computes p-values from conformal prediction intervals.
For each \verb{(cell_id, conf_group, gene)} combination, the p-value is computed as:
\deqn{(1 + \sum \text{covered}) / (1 + n)}
}
